{
  "name": "re-life-admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "prepare": "husky install"
  },
  "dependencies": {
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.2",
    "husky": "^8.0.0",
    "lint-staged": "^13.0.3",
    "typescript": "^4.6.4",
    "vite": "^3.0.6",
    "vue-tsc": "^0.40.1"
  },
  "lint-staged": {
    "*.{js,ts,tsx,jsx,vue}": [
      "prettier --write",
      "git add"
    ]
  }
}
